# Secrets that should NEVER be managed by chezmoi unencrypted
.ssh/id_*
.ssh/*.pem
.ssh/*.key
.ssh/known_hosts
.ssh/config.d/private*
.config/chezmoi/key.txt
.gnupg/
.aws/credentials
.aws/config
.env
.env.*
.netrc
.npmrc
.pypirc

# OS junk
.DS_Store
Thumbs.db

# Non-chezmoi files (stored in repo but not managed as dotfiles)
tampermonkey/
README.md
Brewfile

# Transient state
.bash_history
.zsh_history
.lesshst
.python_history
.wget-hsts

# Neovim config is managed, but its nested starter git checkout is local state.
.config/nvim/.git/**
